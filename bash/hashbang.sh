#!/usr/bin/env bash

# Use the above hashbang for ultimate portability. Because different
# *nuxes put `bash` in different places, using `usr/bin/env` is a workaround
# to run the first `bash` found in the PATH.

# For generic POSIX-compliant scripts, use:

#!/usr/bin/env sh
